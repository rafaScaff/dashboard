# API Configuration
REACT_APP_API_URL=http://localhost:3000
REACT_APP_ENV=development

# Run frontend on port 3001 to avoid conflict with the backend on port 3000
PORT=3001

# Other configuration variables
REACT_APP_DEBUG=true